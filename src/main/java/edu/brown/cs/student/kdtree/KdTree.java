package edu.brown.cs.student.kdtree;

public class KdTree {
  // "However, to make this data structure extensible and reusable,
  // you should write an abstraction, using the principles above,
  // that can handle any number of dimensions."

  // private class Node has k-D point, left neighbor, right neighbor, ____

  // KdTree field: root (Node)
  // KdTree field: keeps track of the depth/relevant axis for next insert
  // KdTree field: number of dimensions (could be an enum?)

  // Kd constructor that takes in an array of k-D points
  // before each insert, sort based on the relevant axis and insert the median for balance
}
